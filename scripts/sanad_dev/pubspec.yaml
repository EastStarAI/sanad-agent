name: sanad_dev
description: Pure-Dart runtime tooling and tests for the sanad-dev CLI.
publish_to: none

environment:
  sdk: ^3.10.7

dependencies:
  ffi: ^2.1.2
  path: ^1.9.0
  sanad_public_service_endpoints:
    path: ../../shared/public_service_endpoints
  sanad_windows_path:
    path: ../../shared/windows_path

dev_dependencies:
  test: ^1.25.0
