name: sanad_release_contract
description: Shared machine-readable Sanad release manifest contract.
version: 1.0.5
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  crypto: ^3.0.7
