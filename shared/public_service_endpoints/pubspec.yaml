name: sanad_public_service_endpoints
description: Canonical public and local Sanad service endpoint selection.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
