name: sanad_auth_lock
description: Cross-process serialization for native Sanad authentication mutations.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
