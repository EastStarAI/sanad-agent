name: sanad_windows_path
description: Windows system PATH resolution shared by the Agent and sanad-dev so spawned processes see OS-level tools.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1