name: sanad_client_identity
description: Privacy-safe stable display references for Sanad Client instances.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  crypto: ^3.0.7

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
